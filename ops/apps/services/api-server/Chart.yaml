apiVersion: v2
name: api-server
description: REST API 서버 — 프로덕션 급 Helm 차트 예시
type: application
version: 0.1.0
appVersion: "1.0.0"

# 의존성 (필요 시)
# dependencies:
#   - name: postgresql
#     version: "15.x.x"
#     repository: oci://registry-1.docker.io/bitnamicharts
#     condition: postgresql.enabled
